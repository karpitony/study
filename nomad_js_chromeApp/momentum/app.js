const player = {
  name: "nico",
  points: 10,
  fat: true,
};
console.log(player);
console.log(player["name"]);

// add new in object
player.lastName = "potato"

player.points += 15
console.log(player)